using System;

public static class LockScreen
{
    public static void UnlockPhone(char[] chars)
    {
        // This code will execute when someone entered an eight digit pin code into the lock screen. Each digit is stored in a separate variable within the array.
        // The values are now passed as a char type. This means, we now get first class language support for characters.
        // With this, the available characters rose to 0-9a-zA-Z

        // Todo: Again, write some code to test if the entered pin equals 3ba8ff01.
        // If so, set Phone.Unlocked = true
        // Otherwise, set Phone.Unlocked = false


    }
}
