using System;

public static class LockScreen
{
    public static void UnlockPhone(UInt16 digit1, UInt16 digit2, UInt16 digit3, UInt16 digit4, UInt16 digit5, UInt16 digit6)
    {
        // This code will execute when someone entered a six digit pin code into the lock screen. Each digit is stored in a separate variable.
        // Also, we will use additional meaning for values greater than ten:
        // a -> 10; b-> 11; c-> 12; d-> 13; e-> 14; f-> 15

        // Todo: Write some code to test if the entered pin equals a0c5f2.
        // If so, set Phone.Unlocked = true
        // Otherwise, set Phone.Unlocked = false


    }
}
